# Changelog

## 2026-09-01 · 40周小记 V0.1 Core

- 将首页改为两个明确入口：记录一件事、快速记录今天的状态。
- 新增 `memory` 与 `status` 两种平台无关的 Record 类型。
- 新增 Core 层：记录模型、日期/孕周工具、校验、状态选项、卡片内容模板和匿名行为事件接口。
- 两种记录共用同一套卡片 Shell：画布比例、奶油色与低饱和绿色像素视觉、标题层级、日期/孕周位置和品牌署名规则保持一致。
- 保留精神状态绿色进度条；用户只选择文字档位，不显示百分比。
- 状态卡继续支持日期、孕周、自由身体感受、活动和 100 字以内的记忆文字。
- 记录卡和状态卡均在浏览器本地生成 PNG；不登录、不上传、不接入数据库或第三方统计服务。
- 移除旧的“单页状态表单直接渲染”流程，改为 Web 表单 → Core Record → Card Content Model → Canvas Renderer。
- 新增小红书容器适配层：在小红书小工具环境中可将生成卡片写入临时文件并调用 `postNote` 发布图文笔记；普通浏览器保留降级提示。
